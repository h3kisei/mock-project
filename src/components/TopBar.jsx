import "../styles/topBar.scss";

const TopBar = () => {
  return (
    <div className="top-bar">
      <span>About Us</span>
      <span>Contacts</span>
      <span>Store</span>
      <span>Track Orders</span>
    </div>
  );
};

export default TopBar;
